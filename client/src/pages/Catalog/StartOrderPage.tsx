import DistributorHomePage from './DistributorHomePage'

/**
 * Start Order = catalog (lane filters + products).
 * Distributor Home shows the stats dashboard instead.
 */
export default function StartOrderPage() {
  return <DistributorHomePage />
}
